// All models have been imported
const { Book } = require("./Book");
const { Transaction } = require("./Transaction");
const { User } = require("./User");

// All models have been exported
module.exports = { Book, Transaction, User };
